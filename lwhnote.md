# Git 个人使用实践笔记（漫步者版）

> 适用于：个人学习、本地开发、备份到 GitHub，避免常见错误

---

## ✅ 核心原则

| 原则 | 说明 |
|------|------|
| **先拉后推** | 每次 `git push` 前，必须先 `git pull --rebase` |
| **保持线性历史** | 用 `--rebase` 替代默认 `merge`，避免杂乱合并提交 |
| **别名清晰** | 远程统一用 `origin`，地址格式：`git@github.com:用户名/仓库名.git` |
| **冲突处理** | rebase 冲突时，手动编辑 → `git add` → `git rebase --continue`，**不要用 `git commit`** |

---

## 🛠️ 标准工作流（每日操作）

```bash
# 1. 修改文件后，暂存
git add .

# 2. 提交（写清晰描述）
git commit -m "描述你的变更"

# 3. 拉取远程更新，并重放本地提交
git pull --rebase origin master

# 4. 推送（首次推送用 -u，之后直接 push）
git push -u origin master   # 第一次用
git push                    # 后续直接用
```

> ✅ 你只需记住这 4 步，即可安全、干净地管理个人项目。

---

## ⚠️ 最易出错的 5 个写法 & 正确做法

| 错误写法 | 错误原因 | 正确做法 |
|----------|----------|----------|
| `git push origin master` | 未设置上游，下次仍报错 | `git push -u origin master`（仅首次） |
| `git remote set-url origin git@github.com:winamd@learngit` | `@` 不是路径分隔符 | `git remote set-url origin git@github.com:winamd/learngit.git` |
| `git pull origin master` | 默认 merge，污染历史 | `git pull --rebase origin master` |
| `git commit` 在 rebase 中途 | 进入 detached HEAD，分支丢失 | `git add <file>` → `git rebase --continue` |
| `git remote add origin ...` 重复执行 | origin 已存在，报错 | 先 `git remote rm origin`，再 `git remote add ...` |

---

## 🔍 常用命令速查

| 目的 | 命令 |
|------|------|
| 查看远程地址 | `git remote -v` |
| 修改远程地址 | `git remote set-url origin git@github.com:xxx/yyy.git` |
| 删除远程 | `git remote remove origin` |
| 添加远程 | `git remote add origin git@github.com:xxx/yyy.git` |
| 查看提交历史（简洁） | `git log --oneline -10` |
| 查看本地与远程差异 | `git fetch origin && git log --oneline -5 HEAD origin/master` |
| 解决 rebase 冲突 | 编辑文件 → `git add <文件>` → `git rebase --continue` |
| 放弃 rebase | `git rebase --abort` |

---

## 📌 关键概念澄清

| 术语 | 说明 |
|------|------|
| `origin` | 远程仓库的**别名**，不是地址。默认由 `git clone` 创建。 |
| `origin/master` | 本地对远程 `master` 的镜像，**不能直接操作**。 |
| `--rebase` | 把你的提交“重放”到远程最新提交之后，历史呈直线。 |
| `fetch` | 只下载远程更新，不改本地文件。 |
| `pull` | = `fetch` + `merge`（默认）或 `fetch` + `rebase`（推荐） |
| `detached HEAD` | 你不在任何分支上，通常是误用 `commit` 在 rebase 中导致。 |

---

## 💡 个人建议

- **GitHub 状态黄色时**：不影响命令执行，只影响速度，耐心等待或稍后重试。
- **仓库名拼写错误**：如 `learngit` 写成 `leargit`，会导致 `fatal: repository not found`，务必核对。
- **SSH 地址格式**：`git@github.com:用户名/仓库名.git`，**不能用 `@` 在路径中**。
- **每次提交前**：先 `git pull --rebase`，再操作，避免冲突。

---

> ✅ 你已掌握 Git 个人使用的全部核心流程。  
> 从此，你不再“学习 Git”，而是**使用 Git**。
```

> 📌 建议：将此文件保存为 `git-cheat-sheet.md`，贴在 Obsidian 或电脑桌面，随时查阅。
```